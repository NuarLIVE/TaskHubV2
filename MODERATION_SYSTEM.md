# AI Система модерации контента

## Статус: ✅ Активна и работает (обновлено 12.11.2025)

Реализована система модерации контента в реальном времени с использованием AI для автоматической проверки всех пользовательских данных.

### Последние исправления:
- ✅ Исправлена работа regex паттернов (убран флаг `g`, оставлен `i`)
- ✅ Русский язык теперь детектируется корректно
- ✅ Исправлена проблема "через раз работает на пустое поле"
- ✅ Модерация работает в чатах (MessagesPage)
- ✅ Состояние корректно сбрасывается при очистке поля
- ✅ **POST-модерация в чатах**: Сообщения удаляются автоматически после отправки, если нарушают правила
- ✅ Плашка предупреждения перемещена НАД полем ввода (вместо под ним)

---

## Как работает модерация

### 1. Модерация в реальном времени
Модерация запускается **автоматически при вводе текста** пользователем:
- При вводе в поле "Заголовок" или "Описание" в заказах/объявлениях
- При вводе текста сообщения в откликах
- При написании сообщений в чате
- Проверка происходит с задержкой 500ms (debounce)

### 2. Что проверяется

#### 🚫 Запрещённый контент (блокировка):
1. **Номера телефонов** - любые телефонные номера (10+ цифр), например: +77975914099
2. **Названия мессенджеров** - Telegram, WhatsApp, Viber, Skype, Discord (проверяется даже одно упоминание!)
3. **Ссылки на другие платформы** - @username с 3+ символами, упоминания соцсетей
4. **Внешние способы оплаты** - PayPal, Qiwi, WebMoney, переводы мимо платформы
5. **Сексуальный контент** - эротика, порно, интимные услуги, нагота
6. **Ненормативная лексика** - маты на русском и английском
7. **Наркотики** - упоминание наркотиков, закладок, курительных смесей

### 3. Действия системы

#### При обнаружении запрещённого контента:
- ⛔ **Кнопка отправки блокируется** (pre-модерация)
- 🚨 **Показывается предупреждение** НАД полем ввода с описанием проблемы
- 📝 **Запись в лог модерации** в базе данных
- ⚠️ **Начисление предупреждения** пользователю

#### POST-модерация в чатах (двойная защита):
Даже если сообщение прошло pre-модерацию (при вводе), оно проверяется **повторно после отправки**:
1. Сообщение отправляется в БД
2. Запускается серверная модерация через Edge Function
3. Если обнаружен запрещённый контент:
   - 🗑️ Сообщение **автоматически удаляется** из БД
   - 🚫 Удаляется из интерфейса пользователя
   - 🚨 Показывается alert с описанием нарушения
   - 📊 Логируется в `moderation_logs` и `user_warnings`

**Это гарантирует**, что никакой запрещённый контент не останется в чатах, даже если обойдена клиентская проверка.

#### Примеры сообщений:
- "Объявление содержит запрещённый контент. Запрещены контактные данные и ссылки на другие платформы."
- "Объявление содержит ненормативную лексику. Пожалуйста, измените его содержание или напишите в поддержку."
- "Объявление содержит запрещённый контент сексуального характера."

---

## Где работает модерация

### ✅ Создание заказа (OrderCreatePage)
- Поля: **Заголовок** + **Описание**
- Модерация: При вводе текста
- Блокировка: Кнопка "Опубликовать"

### ✅ Создание объявления (TaskCreatePage)
- Поля: **Название** + **Описание**
- Модерация: При вводе текста
- Блокировка: Кнопка "Опубликовать"

### ✅ Отправка отклика (ProposalCreate)
- Поле: **Сообщение**
- Модерация: При вводе текста
- Блокировка: Кнопка "Отправить отклик"

### ✅ Сообщения в чате (MessagesPage)
- Поле: **Текст сообщения**
- Модерация: При вводе текста
- Блокировка: Кнопка отправки

---

## Технические детали

### Edge Function: `moderate-content`
Развёрнута на Supabase Edge Functions
- **URL**: `${SUPABASE_URL}/functions/v1/moderate-content`
- **Метод**: POST
- **Авторизация**: Bearer token (JWT)

### Хук: `useContentModeration`
Расположение: `/src/hooks/useContentModeration.ts`

Использование:
```typescript
const { isBlocked, blockMessage, checkContent } = useContentModeration({
  contentType: 'order' | 'task' | 'proposal' | 'message',
  debounceMs: 500,
});

// При вводе текста
onChange={(e) => {
  setValue(e.target.value);
  checkContent(e.target.value);
}}

// Блокировка кнопки
<Button disabled={loading || isBlocked}>
  Отправить
</Button>

// Показ предупреждения
<ModerationAlert message={blockMessage} isVisible={isBlocked} />
```

### Компонент: `ModerationAlert`
Расположение: `/src/components/ui/ModerationAlert.tsx`
Показывает красное предупреждение с описанием проблемы

---

## База данных

### Таблица: `moderation_logs`
Хранит все проверки модерации:
- `user_id` - кто создал контент
- `content_type` - тип контента (order/task/proposal/message)
- `original_content` - проверенный текст
- `flagged` - был ли заблокирован
- `flag_reasons` - причины блокировки
- `confidence_score` - уверенность (0.0 - 1.0)
- `action_taken` - действие (none/warning/blocked)

### Таблица: `user_warnings`
Хранит предупреждения пользователей:
- `user_id` - кому выдано
- `warning_type` - тип нарушения
- `severity` - уровень (1-3)
- `description` - описание

### Поле: `profiles.warning_count`
Счётчик предупреждений пользователя

---

## Паттерны обнаружения

### Телефоны
- `+7 (999) 123-45-67`
- `8-999-123-45-67`
- `+1-234-567-8900`

### Мессенджеры
- `telegram`, `телеграм`, `tg`
- `whatsapp`, `ватсап`
- `viber`, `вайбер`
- `skype`, `скайп`
- `discord`, `дискорд`

### Социальные сети
- `@username`
- Комбинации "пиши в telegram"

### Маты
- Русские: хуй, пизд*, еба*, бля, сука, мудак, пидор
- Английские: fuck, shit, bitch, asshole, dick, pussy

### Сексуальный контент
- Русские: секс, интим, голая/голый, порно, эротика
- Английские: sex, intimate, nude, porn, erotic
- Услуги: эскорт, проститут*, интим-услуг*

---

## Мониторинг и статистика

### Проверка работы модерации:
```sql
-- Последние 10 проверок
SELECT * FROM moderation_logs
ORDER BY created_at DESC
LIMIT 10;

-- Статистика по причинам блокировки
SELECT
  flag_reasons,
  COUNT(*) as count
FROM moderation_logs
WHERE flagged = true
GROUP BY flag_reasons
ORDER BY count DESC;

-- Пользователи с предупреждениями
SELECT
  p.name,
  p.warning_count,
  COUNT(uw.id) as warnings
FROM profiles p
LEFT JOIN user_warnings uw ON uw.user_id = p.id
WHERE p.warning_count > 0
GROUP BY p.id, p.name, p.warning_count
ORDER BY p.warning_count DESC;
```

---

## Что делать если контент заблокирован?

### Для пользователей:
1. Прочитайте сообщение об ошибке
2. Уберите запрещённый контент
3. Попробуйте снова
4. Если не согласны - напишите в поддержку

### Для администраторов:
1. Проверьте лог модерации в `moderation_logs`
2. Оцените правильность срабатывания
3. При необходимости обновите паттерны в Edge Function
4. Задеплойте обновлённую функцию

---

## Обновление паттернов модерации

Файл: `/supabase/functions/moderate-content/index.ts`

1. Обновите нужные паттерны (profanity, phones, external platforms и т.д.)
2. Протестируйте локально
3. Задеплойте:
   ```bash
   # Автоматически через MCP
   # Или вручную если есть Supabase CLI
   ```

---

## FAQ

**Q: Можно ли отключить модерацию?**
A: Да, удалите вызовы `checkContent()` из компонентов и уберите проверку `isBlocked`

**Q: Как добавить новые паттерны?**
A: Обновите функцию `moderateContent()` в Edge Function и задеплойте

**Q: Почему модерация срабатывает на легальный контент?**
A: Ложные срабатывания возможны. Проверьте паттерны и уточните их.

**Q: Можно ли использовать внешний AI?**
A: Да, замените функцию `moderateContent()` на вызов внешнего API (OpenAI, Claude и т.д.)

---

**Статус**: ✅ Работает
**Обновлено**: 12.11.2025
**Версия**: 1.0
